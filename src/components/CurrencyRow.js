import React from "react";

const CurrencyRow = () => {
  return (
    <div>
      <input type="number" className="input" />
      <select>
        <option>Value</option>
      </select>
    </div>
  );
};

export default CurrencyRow;
